---
# Static inventory for --syntax-check and lint only. Nothing here is ever
# connected to: the real inventory is generated by the installer at run time
# into /run/installer/inventory.yml.
k3s_cluster:
  children:
    server:
      hosts:
        node-1.test:
        node-2.test:
        node-3.test:
    agent:
      hosts: {}
  vars:
    ansible_connection: local
    ansible_user: root
